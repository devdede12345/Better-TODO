import React from "react";
import ReactDOM from "react-dom/client";
import StickerApp from "./StickerApp";
import "./sticker.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <StickerApp />
  </React.StrictMode>
);
